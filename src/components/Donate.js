import React, { useState } from 'react';
import DonationForm from './DonationForm';

const Donate = () => {

    return (
        <div>
            <a href='/donationform' className="btn btn-primary"> Want to Help?</a>
        </div>
    );
}

export default Donate;